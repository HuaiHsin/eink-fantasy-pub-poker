# Poker RPG 優化清單

> 對象檔案：`poker eink game 0124.html`
> 整理日期：2026-05-28
> 分類：① 前置修正（影響玩法正確性）② 視覺演出 ③ 內容豐富

---

## ① 前置修正（建議先處理，部分優化項依賴這些）

這些是 code review 找到、會影響玩法正確性的問題，修好後再做下面的內容擴充才不會疊在錯誤的基礎上。

- [x] **戰鬥勝利後缺乏輸入鎖**（`onPlay` 約 line 3679 / `onDiscard` 3557 / `win` 4109）
  勝利後有 3 秒 `setTimeout` 才進獎勵畫面，期間按鈕仍可點，會重複觸發 `win()` → 重複給金幣、疊加獎勵畫面。應在 `win()` 開頭設一個 `battleOver` 旗標並擋掉 `onPlay`/`onDiscard`。
- [x] **小仙女「順子」題常有第二個正解被判錯**（`genPuzzle` 約 line 3239）
  錯誤牌池會包含 `startVal-1`，它同樣能組成順子，玩家選了卻扣 10 HP。
- [x] **小仙女「偶數」題固定塞入 A，但 A 點數=14 是偶數**（line 3261）
  `oddRanks` 把 A 當奇數，但 `Data.VALUES['A']=14`。每題都把 A 當錯誤牌，玩家用正確數學判斷反而被判錯。
- [x] **鬼魅卡（spectral）在順子判定中失效**（`checkStraight` 3761 vs `evalHand` 3767）
  `evalHand` 把 spectral 當萬能牌算同花/同點，但 `checkStraight` 只認 `JO`，導致用鬼魅卡補順子會失敗。
- [x] **遺物「小狗骨頭」(r_bone) 的 +5% 閃避未實作**（line 3738 / 4911）
  閃避判定只認 `r_boot` 且機率寫死 0.1，r_bone 的 `dodge:0.05` 從未被讀取。
- [x] **詞綴敵人的中文名掉了形容詞**（`genEnemy` line 2585）
  `nameZh` 被重設為 `prefix+noun`，少了 `adj`；英文名卻保留。中文玩家看到的名稱較不完整。

---

## ② 視覺演出（呈現層）

### E-ink 特性強化
- [x] **新增「效能模式」開關**：停用所有 `transition` / `animation`（`hp-fill` 的 `width 0.2s`、`damage-popup` 飄動等），改為瞬間切換 + 反白閃爍。電子紙重繪慢，動畫會殘影。
- [x] **黑白模式下的傷害數字**：`damage-popup` 多層 `text-shadow` 描邊 + 位移動畫在黑白機上會糊。改為「定點放大 → 0.3s 後消失」，少動多閃。

### 戰鬥可讀性
- [x] **關鍵事件強調**：所有訊息都擠在單行 `#msg-box`，光環激活 / 場地觸發 / 附魔碎裂等容易被錯過。重要事件改成「整條訊息列反白閃一下」，而非只換文字。
- [x] **敵人蓄力警告**：敵人出牌前只有 `MSG_WAIT` 文字。當 `e.cd <= 1` 時讓敵人顏文字或 CD 標籤閃爍，凸顯重擊型（🐢 heavy，CD 5）的張力。

### 現有系統的視覺缺口
- [x] **場地氛圍**：濃霧 / 荊棘 / 重力 / 暴風目前只有卡牌徽章與文字。依 `field.special` 在 `.zone-table` 疊一層 CSS 紋理（霧=半透明遮罩、荊棘=尖刺邊框、重力=底部陰影、暴風=斜線）。
- [x] **敵人型態辨識度**：archetype 已有 ⚡🐢🔮 圖示，但和 prefix / trait 標籤混在一起。把型態做成主色或專屬邊框，讓玩家一眼分辨敵人類型。

---

## ③ 內容豐富（玩法層）

### 深化既有機制
- [x] **擴充附魔種類**：修好 spectral 順子 bug 後，新增條件型附魔（例：「每回合首張同花 +mult」），讓構築流派成形。
- [x] **條件觸發型遺物**：目前遺物多為 +chip / +mult / +hp。加入「手牌為順子時額外 +50%」「HP < 30% 時傷害翻倍」等條件型，與現有光環（疾風 / 元素 / 迴聲）呼應。

### 敵人 AI 與 Boss
- [x] **補強敵人出牌邏輯**：`aiFindBestMatch` 只會湊同點數（對子 / 三條），永遠不出順子或同花，所以 MAX_HAND 並不真的最強。補上順子 / 同花搜尋，高層敵人才有壓迫感。
- [x] **分段關底 Boss**：`SCENE_DECORATORS` 已分 5 段（森林 / 洞窟 / 城堡 / 火山 / 虛空）。為每段設計一隻有獨特機制的關底，讓世界觀有層次。

### 事件與長線目標
- [x] **跨層連鎖事件**：目前多為二選一即時結果。「小狗報恩」已是雛形（`puppyHealed`），可擴充為前面選擇影響後面的連鎖事件，增加重玩價值。
- [x] **Meta 進度系統**：每局結束即重置。加入 localStorage 存檔的解鎖系統（新遺物 / 新事件 / 新起始牌組），提升長期黏著。

---

## ④ 佈局與排版優化

- [x] **縮減玩家 HP 條與道具/遺物列之間的過大空白**：移除 `#p-buffs` 與 `#p-auras` 的 `min-height`，並實作 CSS `:empty` 自動隱藏，使無狀態時自動摺疊，版面更加緊湊。
- [x] **小狗射龍門上方卡牌裝飾精簡**：移除上方裝飾列中容易造成換行跑版與空間佔用的上下卡牌 `🎴`，僅保留中央單張卡牌與小狗動畫，顯著縮小垂直佔用空間。
- [x] **事件大顏文字表情強制防折行**：為事件 NPC 顏文字頭像容器 `.event-image` 新增 `white-space: nowrap` 並微調字型大小至 `48px`，徹底解決包含空白字元的顏文字（如怪醫 `( ⚆ ᴥ ⚆ )`）在手機或窄版螢幕因字型寬度不足被迫折行分裂的跑版問題。

---

## 建議起手順序

1. 先清 **①前置修正**（尤其勝利輸入鎖，影響金幣經濟）。
2. 視覺面從 **黑白模式效能開關** 入手（最貼合 e-ink 定位，改動可控）。
3. 內容面從 **敵人 AI 出牌邏輯** 入手（直接提升戰鬥手感）。
